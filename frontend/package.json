{
  "name": "photo-manager-frontend",
  "private": true,
  "dependencies": {
    "electrobun": "^1.15.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  },
  "scripts": {
    "dev": "electrobun build && electrobun dev",
    "build": "electrobun build",
    "build:spa": "bun build src/app/main.ts --outdir dist/ --sourcemap=none && cp src/app/index.html dist/index.html && cp src/app/style.css dist/style.css 2>/dev/null || true && printf '{\"v\":\"%s\"}' \"$(date '+%H:%M %d/%m/%y')\" > dist/version.json"
  }
}
